[project]
name = "matscout"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "PRINCY SONI", email = "soniprincy02@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "crawl4ai>=0.9.2",
    "httpx>=0.28.1",
    "langchain>=1.3.15",
    "langchain-ollama>=1.1.0",
    "langgraph>=1.2.11",
    "ollama>=0.6.2",
    "pydantic>=2.13.4",
]

[project.scripts]
matscout = "matscout:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
